"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import type { InventoryItem, InventoryStatus } from "@/types/inventory";
const statuses: InventoryStatus[] = ["planned", "available", "reserved", "sold", "damaged", "lost"];
export function InventoryControls({ item }: { item: InventoryItem }) { const [quantity, setQuantity] = useState(String(item.quantity)); const [status, setStatus] = useState<InventoryStatus>(item.status); const [warehouse, setWarehouse] = useState(item.warehouse || ""); const [shelf, setShelf] = useState(item.shelf || ""); const [bin, setBin] = useState(item.bin || ""); const [saving, setSaving] = useState(false); const router = useRouter(); async function save() { setSaving(true); await fetch(`/api/inventory/${item.id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ quantity: Number(quantity) || 0, status, warehouse, shelf, bin }) }); setSaving(false); router.refresh(); } return <div className="mt-3 grid gap-2 sm:grid-cols-[80px_130px_1fr_1fr_1fr_auto]"><input aria-label="Quantity" type="number" min="0" value={quantity} onChange={(event) => setQuantity(event.target.value)} placeholder="Qty" className="rounded border border-border bg-background px-2 py-1 text-sm" /><select value={status} onChange={(event) => setStatus(event.target.value as InventoryStatus)} className="rounded border border-border bg-background px-2 py-1 text-sm capitalize">{statuses.map((option) => <option key={option}>{option}</option>)}</select><input value={warehouse} onChange={(event) => setWarehouse(event.target.value)} placeholder="Warehouse" className="rounded border border-border bg-background px-2 py-1 text-sm" /><input value={shelf} onChange={(event) => setShelf(event.target.value)} placeholder="Shelf" className="rounded border border-border bg-background px-2 py-1 text-sm" /><input value={bin} onChange={(event) => setBin(event.target.value)} placeholder="Bin" className="rounded border border-border bg-background px-2 py-1 text-sm" /><button onClick={save} disabled={saving} className="rounded bg-violet-600 px-3 py-1 text-sm font-medium text-white disabled:opacity-60">{saving ? "Saving…" : "Update"}</button></div>; }
