"use client";
import { useState } from "react";
import type { Marketplace } from "@/domain/business";
type Preview = { batchId: string; accepted: number; rejected: number; errors: { row: number; message: string }[]; duplicates: string[]; reviews: { id: string; itemTitle: string; externalOrderId: string; externalSku?: string; likelyVariantIds: string[]; matchConfidence: number }[] };
export function OrderImportWorkspace() { const [marketplace, setMarketplace] = useState<Marketplace>("Manual"); const [preview, setPreview] = useState<Preview>(); const [message, setMessage] = useState(""); const [busy, setBusy] = useState(false); async function upload(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); form.set("marketplace", marketplace); setBusy(true); setMessage("Parsing and validating CSV…"); try { const response = await fetch("/api/orders/import", { method: "POST", body: form }); const data = await response.json(); if (!response.ok) throw new Error(data.message); setPreview(data); setMessage(data.errors.length || data.duplicates.length || data.reviews.length ? "Resolve all row issues before confirmation." : "Ready to confirm import."); } catch (error) { setMessage(error instanceof Error ? error.message : "Import preview failed."); } finally { setBusy(false); } }
return <section className="border border-border bg-card p-5"><h2 className="font-semibold">CSV import and review queue</h2><p className="mt-1 text-sm text-muted-foreground">Upload first; confirmation remains locked until duplicate and unmatched rows are resolved.</p><form className="mt-4 flex flex-wrap gap-3" onSubmit={upload}><input required name="file" type="file" accept=".csv,text/csv" className="text-sm" /><select value={marketplace} onChange={(event) => setMarketplace(event.target.value as Marketplace)} className="border bg-background p-2 text-sm">{["Manual","Depop","eBay","Etsy","Mercari","Poshmark"].map((name) => <option key={name}>{name}</option>)}</select><button disabled={busy} className="bg-emerald-500 px-3 text-sm font-semibold text-zinc-950">{busy ? "Validating…" : "Preview CSV"}</button></form>{message && <p role="status" className="mt-3 text-sm text-muted-foreground">{message}</p>}{preview && <div className="mt-4 grid gap-4 md:grid-cols-3"><div><b>Batch {preview.batchId.slice(0,8)}</b><p className="text-sm">Accepted {preview.accepted} · Rejected {preview.rejected}</p><p className="text-sm">Duplicates {preview.duplicates.length} · Review {preview.reviews.length}</p></div><div className="text-xs"><b>Row errors</b>{preview.errors.map((error) => <p key={error.row}>Row {error.row}: {error.message}</p>)}{preview.duplicates.map((id) => <p key={id}>Duplicate order: {id}</p>)}</div><div className="text-xs"><b>Unmatched-item review</b>{preview.reviews.map((review) => <p key={review.id}>{review.externalOrderId} · {review.itemTitle} · SKU {review.externalSku || "none"} · {Math.round(review.matchConfidence * 100)}% match</p>)}{!preview.reviews.length && <p>All item lines matched.</p>}</div></div>}</section>; }
