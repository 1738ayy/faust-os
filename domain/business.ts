export type RecordStatus = "draft" | "active" | "paused" | "completed" | "cancelled" | "attention";
export type OrderStatus = "draft" | "pending_payment" | "paid" | "confirmed" | "reserved" | "ready_to_pack" | "packed" | "label_created" | "ready_to_ship" | "shipped" | "in_transit" | "delivered" | "closed" | "cancelled" | "return_requested" | "return_in_transit" | "returned" | "partially_refunded" | "refunded" | "inventory_reserved" | "label_purchased";

export type Product = { id: string; title: string; brand?: string; category: string; tags: string[]; supplierId?: string; sourceUrl?: string; image?: string; status: RecordStatus; createdAt: string; updatedAt: string };
export type Variant = { id: string; productId: string; sku: string; title: string; condition: string; landedUnitCost: number; defaultSalePrice: number; weightOz?: number; reorderPoint: number; reorderQuantity: number; active: boolean };
export type Location = { id: string; label: string; warehouse: string; zone?: string; aisle?: string; shelf?: string; bin?: string };
export type StockBalance = { id: string; variantId: string; locationId?: string; onHand: number; reserved: number; incoming: number; damaged: number; returned: number; lost: number; quarantined: number };
export type StockMovement = { id: string; variantId: string; quantity: number; type: "purchase_received" | "manual_adjustment" | "order_reservation" | "order_cancellation" | "sale" | "return" | "damage" | "loss" | "transfer" | "cycle_count"; referenceType?: string; referenceId?: string; note?: string; onHandDelta?: number; reservedDelta?: number; sourceLocationId?: string; destinationLocationId?: string; actorId?: string; createdAt: string };
export type Supplier = { id: string; name: string; contact?: string; email?: string; sourcePlatform: string; leadDays?: number; rating?: number; status: RecordStatus; notes?: string };
export type PurchaseOrderItem = { id: string; variantId: string; expectedQuantity: number; receivedQuantity: number; unitCost: number };
export type PurchaseOrder = { id: string; supplierId: string; reference: string; status: "draft" | "ordered" | "partial" | "received" | "issue"; orderedAt: string; expectedAt?: string; totalCost: number; itemCount: number; items: PurchaseOrderItem[]; parcelId?: string };
export type ParcelItem = { id: string; variantId: string; purchaseOrderItemId?: string; expectedQuantity: number; receivedQuantity: number };
export type Parcel = { id: string; trackingNumber: string; carrier?: string; status: "awaiting_dispatch" | "warehouse" | "in_transit" | "customs" | "out_for_delivery" | "delivered" | "delayed"; origin?: string; destination?: string; estimatedArrival?: string; purchaseOrderId?: string; items: ParcelItem[]; events: { label: string; timestamp: string; location?: string }[] };
export type Marketplace = "Depop" | "eBay" | "Etsy" | "Mercari" | "Poshmark" | "Manual";
export type Listing = { id: string; variantId: string; marketplace: Marketplace; title: string; price: number; quantity: number; status: "draft" | "ready" | "active" | "paused" | "sold" | "failed" | "manual_publish"; marketplaceUrl?: string; views?: number; likes?: number; syncState: "manual" | "connected" | "error"; createdAt: string };
export type Customer = { id: string; name: string; email?: string; city?: string; state?: string; orderCount: number; lifetimeValue: number; issueCount: number };
export type OrderItem = { id: string; productId?: string; variantId: string; listingId?: string; title: string; quantity: number; unitSellingPrice: number; discountAllocation: number; taxAllocation: number; marketplaceFeeAllocation?: number; paymentFeeAllocation?: number; feeAllocation: number; unitCost: number; profitContribution?: number; fulfillmentState?: "unfulfilled" | "reserved" | "packed" | "shipped" | "cancelled" | "returned"; returnState?: "none" | "requested" | "in_transit" | "received" | "rejected"; refundState?: "none" | "partial" | "refunded"; refundedQuantity?: number; returnedQuantity?: number; cancelledQuantity?: number };
export type OrderStatusEvent = { id: string; fromStatus?: OrderStatus; toStatus: OrderStatus; detail?: string; createdAt: string };
export type OrderRefund = { id: string; amount: number; reason: string; refundedAt: string; externalRefundId?: string; itemId?: string; shippingAmount?: number; feeAdjustment?: number };
export type OrderReturn = { id: string; status: "requested" | "approved" | "in_transit" | "received" | "closed" | "rejected"; reason: string; trackingNumber?: string; items: { orderItemId: string; quantity: number; disposition?: "available" | "damaged" | "quarantine" }[]; createdAt: string; receivedAt?: string };
export type OrderAddress = { name?: string; line1: string; line2?: string; city: string; region?: string; postalCode: string; country: string };
export type Order = { id: string; number: string; marketplace: Marketplace; customerId: string; items: OrderItem[]; shippingCharged: number; shippingCost: number; packagingCost?: number; advertisingCost?: number; marketplaceFee: number; paymentFee: number; taxCollected: number; status: OrderStatus; orderedAt: string; paidAt?: string; trackingNumber?: string; shippingLabelUrl?: string; shipBy?: string; notes?: string; tags?: string[]; shippingAddress?: OrderAddress; billingAddress?: OrderAddress; idempotencyKey?: string; statusEvents?: OrderStatusEvent[]; refunds?: OrderRefund[]; returns?: OrderReturn[] };
export type FinanceTransactionType = "sale" | "refund" | "marketplace_fee" | "payment_fee" | "shipping_revenue" | "shipping_expense" | "packaging" | "inventory_purchase" | "freight" | "duty" | "software" | "advertising" | "owner_contribution" | "owner_withdrawal" | "transfer" | "debt_payment" | "tax_reserve_movement" | "payout" | "adjustment" | "shipping" | "inventory" | "expense" | "tax_reserve";
export type FinanceSourceType = "order" | "order_item" | "refund" | "return" | "shipment" | "purchase_order" | "payout" | "expense" | "budget" | "tax_reserve" | "reinvestment" | "manual";
export type Transaction = { id: string; type: FinanceTransactionType; amount: number; status: "pending" | "cleared"; occurredAt: string; orderId?: string; orderItemId?: string; purchaseOrderId?: string; payoutId?: string; expenseId?: string; sourceType?: FinanceSourceType; sourceId?: string; sourceAccountId?: string; destinationAccountId?: string; linkedObjectType?: FinanceSourceType; linkedObjectId?: string; description: string; category: string; accountId?: string; externalId?: string; idempotencyKey?: string; audit?: string[] };
export type FinancialAccount = { id: string; name: string; type: "cash" | "bank" | "marketplace_clearing" | "credit_card" | "tax_reserve" | "owner_equity"; currency: "USD"; openingBalance: number; currentBalance: number; status: "active" | "archived"; createdAt: string; updatedAt?: string };
export type FinanceCategory = { id: string; name: string; type: "income" | "cogs" | "fee" | "expense" | "asset" | "liability" | "equity" | "tax" | "transfer"; parentId?: string; taxDeductible?: boolean; createdAt: string; updatedAt?: string };
export type Payout = { id: string; marketplace: Marketplace; accountId?: string; status: "expected" | "received" | "reconciled" | "discrepancy" | "cancelled" | "archived"; expectedAmount: number; actualAmount?: number; fees: number; adjustments: number; orderIds: string[]; transactionIds: string[]; expectedAt?: string; receivedAt?: string; externalPayoutId?: string; discrepancyAmount?: number; notes?: string; audit?: string[]; archivedAt?: string; createdAt: string; updatedAt?: string };
export type PayoutReconciliation = { id: string; payoutId: string; status: "open" | "matched" | "discrepancy" | "resolved" | "reopened"; expectedAmount: number; actualAmount: number; discrepancyAmount: number; resolution?: string; includedOrderIds: string[]; feeTransactionIds: string[]; adjustmentTransactionIds: string[]; audit: string[]; createdAt: string; updatedAt?: string };
export type Expense = { id: string; vendor: string; category: string; amount: number; date: string; recurring: "none" | "weekly" | "monthly" | "annual"; taxDeductible: boolean; receiptStatus: "not_required" | "pending_attachment" | "attached"; supplierId?: string; orderId?: string; notes?: string; transactionId?: string; audit?: string[]; archivedAt?: string; deletedAt?: string; createdAt: string; updatedAt?: string };
export type Budget = { id: string; month: string; category: string; amount: number; actualAmount: number; remainingAmount: number; alertThreshold: number; status: "on_track" | "near_limit" | "overspent"; templateName?: string; rolloverFromBudgetId?: string; audit?: string[]; createdAt: string; updatedAt?: string };
export type TaxReserveMovement = { id: string; amount: number; basisAmount: number; rate: number; sourceType: FinanceSourceType; sourceId: string; status: "reserved" | "released" | "paid" | "adjusted"; createdAt: string; notes?: string; audit?: string[] };
export type ReinvestmentAllocation = { id: string; target: "inventory" | "shipping_reserve" | "marketing" | "operating_reserve" | "tax_reserve" | "owner_distribution"; percentage: number; amount: number; basis: "deployable_cash" | "gross_profit" | "contribution_profit"; recommendation?: string; approvedAt?: string; approvalHistory?: string[]; createdAt: string; updatedAt?: string };
export type ForecastScenario = { id: string; name: "best" | "expected" | "worst" | string; revenueMultiplier: number; expenseMultiplier: number; confidence: number; assumptions: string[] };
export type Forecast = { id: string; period: string; revenue: number; contributionProfit: number; operatingProfit: number; cash: number; inventoryPurchasing: number; payouts: number; stockouts: number; reorderCosts: number; confidence: number; assumptions: string[]; scenarios?: ForecastScenario[]; selectedScenarioId?: string; createdAt: string; updatedAt?: string };
export type Task = { id: string; title: string; status: "open" | "done"; priority: "critical" | "high" | "normal"; dueAt?: string; entityType?: string; entityId?: string; createdAt: string };
export type Notice = { id: string; severity: "critical" | "warning" | "info"; title: string; detail: string; actionLabel: string; href: string; createdAt: string; resolved?: boolean; read?: boolean; archived?: boolean; category?: "orders" | "inventory" | "shipping" | "finance" | "system"; entityType?: string; entityId?: string };
export type Insight = { id: string; type: "cash" | "inventory" | "shipping" | "listing" | "profit"; title: string; summary: string; evidence: string; actionLabel: string; href: string; createdAt: string };
export type Activity = { id: string; action: string; entityType: string; entityId: string; detail: string; createdAt: string };
export type OrderImportReview = { id: string; batchId?: string; rowNumber?: number; marketplace: Marketplace; externalOrderId: string; externalListingId?: string; externalSku?: string; itemTitle: string; quantity: number; unitPrice: number; likelyVariantIds: string[]; matchConfidence: number; state: "open" | "linked" | "ignored" | "non_inventory" | "cancelled" | "resolved"; resolution?: "variant" | "listing" | "created_variant" | "non_inventory" | "ignored" | "cancelled"; resolvedVariantId?: string; resolvedListingId?: string; createdProductId?: string; createdVariantId?: string; createdAt: string; updatedAt?: string };
export type SavedOrderView = { id: string; name: string; filters: { status?: OrderStatus; marketplace?: Marketplace; query?: string; negativeMargin?: boolean; refundState?: boolean; returnState?: boolean; issueState?: boolean; importReviewState?: boolean }; isDefault?: boolean; position?: number; createdAt: string; updatedAt?: string };
export type OrderImportBatchRow = { id: string; rowNumber: number; externalOrderId: string; marketplace: Marketplace; customerName: string; email?: string; externalSku?: string; externalListingId?: string; itemTitle: string; quantity: number; unitPrice: number; sourceValues: Record<string, string | number | undefined>; status: "accepted" | "rejected" | "review_required" | "resolved" | "ignored" | "cancelled" | "imported" | "failed"; error?: string; reviewId?: string; orderId?: string; retryable?: boolean; retryCount?: number; lastAttemptedAt?: string };
export type OrderImportBatch = { id: string; filename: string; marketplace: Marketplace; mapping: string[]; status: "preview" | "review_required" | "ready" | "completed" | "partially_failed" | "failed" | "cancelled" | "archived"; rows: OrderImportBatchRow[]; acceptedRows: number; rejectedRows: number; unresolvedRows: number; failedRows: number; importedOrders: number; createdOrderIds: string[]; createdAt: string; updatedAt: string; confirmedAt?: string; archivedAt?: string };
export type FulfillmentStatus = "ready_to_pick" | "picking" | "ready_to_pack" | "packing" | "packed" | "ready_to_ship" | "printed" | "manifested" | "dispatched" | "in_transit" | "delivered" | "returned" | "lost" | "claim_filed" | "exception" | "awaiting_inventory" | "awaiting_payment";
export type FulfillmentPickItem = { id: string; orderItemId: string; variantId: string; locationId?: string; sku: string; title: string; quantity: number; status: "open" | "picked" | "missing" | "substituted" | "damaged"; notes?: string };
export type FulfillmentPickList = { id: string; orderIds: string[]; mode: "single" | "batch" | "zone"; zone?: string; picker?: string; status: "open" | "picking" | "completed" | "exception"; path: string[]; items: FulfillmentPickItem[]; createdAt: string; updatedAt: string; completedAt?: string };
export type FulfillmentAddressValidation = { status: "valid" | "invalid" | "warning" | "overridden"; original: OrderAddress; suggested?: OrderAddress; warnings: string[]; residential: boolean; confirmedAt?: string; overrideReason?: string };
export type FulfillmentRate = { id: string; provider: "local_mock" | "manual_label" | "marketplace_label" | "easypost" | "shippo"; carrier: string; service: string; deliveryDays: number; retailRate: number; negotiatedRate?: number; currency: "USD"; insuranceAvailable: boolean; signatureAvailable: boolean; warnings: string[]; packageWeightOz: number; dimensions: { lengthIn: number; widthIn: number; heightIn: number } };
export type FulfillmentLabel = { id: string; provider: "local_mock" | "manual_label" | "marketplace_label" | "easypost" | "shippo"; carrier: string; service: string; trackingNumber: string; labelUrl: string; format: "4x6" | "letter"; status: "active" | "voided" | "refunded" | "regenerated"; postageCost: number; createdAt: string; voidedAt?: string; regeneratedFromLabelId?: string; source: "mock" | "manual_upload" | "marketplace" | "provider" };
export type FulfillmentPackage = { id: string; type: "poly_mailer" | "box" | "custom"; weightOz: number; lengthIn: number; widthIn: number; heightIn: number; preset?: string; mergedFrom?: string[]; splitFrom?: string; notes?: string; photoAttachmentPending?: boolean };
export type FulfillmentShipmentEvent = { id: string; status: FulfillmentStatus; label: string; timestamp: string; location?: string };
export type FulfillmentShipment = { id: string; orderId: string; status: FulfillmentStatus; priority: "standard" | "high" | "urgent"; shippingMethod: string; picker?: string; packer?: string; station?: string; assigneeReleasedAt?: string; hold?: { active: boolean; reason?: string; createdAt?: string; releasedAt?: string }; slaDeadline?: string; shippingProvider?: "local_mock" | "manual_label" | "marketplace_label" | "easypost" | "shippo"; addressValidation?: FulfillmentAddressValidation; rateState?: "idle" | "loading" | "provider_unavailable" | "no_rates" | "invalid_package" | "address_issue" | "success"; rates?: FulfillmentRate[]; selectedRateId?: string; carrier?: string; service?: string; trackingNumber?: string; labelUrl?: string; labelFormat?: "4x6" | "letter"; postageCost?: number; labelHistory?: FulfillmentLabel[]; manifestId?: string; estimatedDelivery?: string; actualDelivery?: string; trackingStatus?: "pre_transit" | "in_transit" | "delivered" | "delayed" | "returned" | "lost" | "claim_required"; lastScan?: string; packageIds: string[]; packages: FulfillmentPackage[]; scanLog: { sku: string; quantity: number; verifiedAt: string; warning?: string }[]; timestamps: Partial<Record<FulfillmentStatus, string>>; events: FulfillmentShipmentEvent[]; createdAt: string; updatedAt: string };
export type FulfillmentManifest = { id: string; carrier: string; status: "open" | "closed" | "dispatched" | "reopened"; shipmentIds: string[]; labelCount: number; generatedAt: string; closedAt?: string; dispatchedAt?: string; handoffConfirmedAt?: string; activity: string[] };
export type FulfillmentException = { id: string; orderId?: string; shipmentId?: string; type: "missing_inventory" | "damaged_package" | "failed_scan" | "duplicate_shipment" | "invalid_address" | "label_failure" | "returned_to_sender" | "carrier_delay" | "lost_shipment" | "customer_hold" | "manual_review"; severity: "critical" | "warning" | "info"; owner?: string; notes: string; status: "open" | "resolved"; activity: string[]; slaDeadline?: string; notificationId?: string; createdAt: string; resolvedAt?: string; reopenedAt?: string };
export type LandedCostAllocationMethod = "by_quantity" | "by_value" | "by_weight" | "manual";
export type InventoryPurchaseBatch = { id: string; supplierId?: string; purchaseOrderId?: string; reference: string; currency: "USD" | "RMB"; exchangeRateId?: string; status: "draft" | "received" | "costed" | "closed" | "cancelled"; itemCount: number; subtotalOriginal: number; subtotalUsd: number; landedCostUsd: number; totalCostUsd: number; receivedAt: string; idempotencyKey?: string; createdAt: string; updatedAt?: string };
export type InventoryLot = { id: string; batchId?: string; variantId: string; sku: string; physicalSku?: string; quantityReceived: number; quantityRemaining: number; quantityReserved: number; unitProductCostUsd: number; unitLandedCostUsd: number; totalLandedCostUsd: number; currency: "USD" | "RMB"; originalUnitCost: number; exchangeRate: number; locationId?: string; condition: "available" | "returned_goods" | "damaged" | "quarantine"; sourceType: "purchase_batch" | "return" | "adjustment"; sourceId?: string; receivedAt: string; createdAt: string; updatedAt?: string };
export type LandedCostComponent = { id: string; batchId: string; type: "product" | "domestic_shipping" | "international_freight" | "duty" | "tax" | "agent_fee" | "inspection" | "packaging" | "other"; description: string; amountOriginal: number; currency: "USD" | "RMB"; amountUsd: number; allocationMethod: LandedCostAllocationMethod; linkedObjectType?: "purchase_order" | "parcel" | "supplier_invoice" | "manual"; linkedObjectId?: string; createdAt: string };
export type ExchangeRateRecord = { id: string; baseCurrency: "RMB"; quoteCurrency: "USD"; rate: number; effectiveAt: string; source: "manual" | "provider" | "invoice"; createdAt: string };
export type OrderItemCostAllocation = { id: string; orderId: string; orderItemId: string; variantId: string; lotId: string; quantity: number; unitCostUsd: number; totalCostUsd: number; method: "fifo" | "manual"; returnId?: string; idempotencyKey?: string; createdAt: string };
export type JournalEntry = { id: string; entryNumber: string; status: "posted" | "voided"; sourceType: "purchase_batch" | "order_item_allocation" | "return" | "finance_transaction" | "manual"; sourceId: string; description: string; totalDebit: number; totalCredit: number; idempotencyKey?: string; createdAt: string; postedAt: string };
export type JournalLine = { id: string; journalEntryId: string; accountCode: string; accountName: string; debit: number; credit: number; sourceType?: string; sourceId?: string; createdAt: string };
export type TransactionalOutboxEvent = { id: string; topic: "inventory.lot.received" | "inventory.fifo.allocated" | "inventory.return.received" | "channel.inventory.sync_requested" | "finance.journal.posted" | "listing.publish_requested" | "listing.quantity_sync_requested" | "listing.delist_requested" | "listing.sold_coordination_requested"; aggregateType: string; aggregateId: string; payload: Record<string, unknown>; status: "pending" | "processing" | "published" | "failed" | "dead_lettered"; attempts: number; nextAttemptAt?: string; idempotencyKey?: string; createdAt: string; updatedAt?: string };
export type DurableJob = { id: string; queue: "channel_sync" | "ledger_posting" | "inventory_risk" | "marketplace_publish"; eventId?: string; status: "queued" | "running" | "succeeded" | "failed" | "dead_lettered"; attempts: number; maxAttempts: number; payload: Record<string, unknown>; lastError?: string; runAfter: string; createdAt: string; updatedAt?: string };
export type DeadLetterEvent = { id: string; sourceType: "outbox_event" | "durable_job"; sourceId: string; reason: string; payload: Record<string, unknown>; createdAt: string };
export type ChannelInventorySyncState = { id: string; channel: Marketplace; listingId: string; variantId: string; physicalSku?: string; desiredQuantity: number; lastSyncedQuantity?: number; status: "clean" | "pending" | "blocked" | "error"; risk: "none" | "oversell" | "stale" | "unmapped"; lastSyncAt?: string; nextSyncAt?: string; error?: string; updatedAt: string };
export type InventoryRiskLock = { id: string; variantId: string; listingId?: string; channel?: Marketplace; reason: "oversell_risk" | "unmapped_sku" | "stale_sync" | "manual_hold"; status: "active" | "released"; lockedQuantity: number; createdAt: string; releasedAt?: string; notes?: string };
export type PhysicalSkuMapping = { id: string; variantId: string; physicalSku: string; channelListingId?: string; channel?: Marketplace; externalSku?: string; externalListingId?: string; status: "active" | "needs_review" | "archived"; confidence: number; createdAt: string; updatedAt?: string };
export type MarketplaceAccount = { id: string; marketplace: Exclude<Marketplace, "Manual">; displayName: string; status: "manual" | "extension_assisted" | "adapter_ready" | "credentials_required" | "connected" | "paused"; supportsApiPublish: boolean; supportsExtension: boolean; lastSyncAt?: string; createdAt: string; updatedAt?: string };
export type ListingTemplate = { id: string; name: string; marketplace: Marketplace; category: string; titleFormat: string; descriptionFormat: string; priceAdjustmentPercent: number; defaultAttributes: Record<string, string>; imagePolicy: "all" | "first_four" | "square_crop"; shippingProfile?: string; createdAt: string; updatedAt?: string };
export type ChannelListingDraft = { id: string; listingId: string; variantId: string; physicalSku: string; marketplace: Exclude<Marketplace, "Manual">; accountId?: string; templateId?: string; title: string; description: string; price: number; category: string; attributes: Record<string, string>; imageUrls: string[]; quantity: number; status: "draft" | "validated" | "queued" | "published" | "manual_required" | "paused" | "delisted" | "sold" | "failed"; validationErrors: string[]; publishMode: "adapter" | "extension" | "manual"; externalListingId?: string; externalUrl?: string; lastSyncAt?: string; syncState: "clean" | "pending" | "risk_locked" | "failed" | "manual"; riskLockId?: string; idempotencyKey?: string; createdAt: string; updatedAt?: string };
export type ListingSyncJob = { id: string; channelDraftId: string; marketplace: Exclude<Marketplace, "Manual">; action: "publish" | "pause" | "delist" | "sync_quantity" | "confirm_external" | "sold_coordination" | "retry"; status: "queued" | "running" | "succeeded" | "failed" | "dead_lettered" | "manual_required"; attempts: number; maxAttempts: number; error?: string; idempotencyKey?: string; runAfter: string; createdAt: string; updatedAt?: string };
export type ListingReviewItem = { id: string; channelDraftId?: string; marketplace: Exclude<Marketplace, "Manual">; severity: "critical" | "warning" | "info"; reason: "validation_error" | "manual_publish_required" | "sync_failed" | "risk_lock" | "external_confirmation_required" | "sold_coordination"; status: "open" | "resolved" | "archived"; detail: string; actionLabel: string; createdAt: string; resolvedAt?: string; archivedAt?: string };
export type SupplierContact = { id: string; supplierId: string; name: string; role?: string; channel: "1688" | "wechat" | "email" | "whatsapp" | "phone" | "other"; handle?: string; preferred?: boolean; createdAt: string; updatedAt?: string };
export type SupplierCommunication = { id: string; supplierId: string; contactId?: string; purchaseOrderId?: string; channel: SupplierContact["channel"]; direction: "inbound" | "outbound" | "internal_note"; subject: string; body: string; occurredAt: string; createdAt: string };
export type SupplierScorecard = { id: string; supplierId: string; qualityScore: number; leadTimeScore: number; communicationScore: number; priceScore: number; defectRate: number; onTimeRate: number; averageLeadDays: number; totalSpendUsd: number; claimCount: number; lastReviewedAt: string; updatedAt: string };
export type PurchaseApproval = { id: string; purchaseOrderId: string; status: "requested" | "approved" | "rejected"; requestedBy?: string; approvedBy?: string; reason?: string; requestedAt: string; decidedAt?: string };
export type PurchasePayment = { id: string; purchaseOrderId: string; supplierId: string; type: "deposit" | "final" | "freight" | "duty" | "customs" | "refund"; currency: "USD" | "RMB"; amountOriginal: number; exchangeRate: number; amountUsd: number; status: "planned" | "paid" | "refunded"; paidAt?: string; transactionId?: string; idempotencyKey?: string; createdAt: string };
export type FreightConsolidation = { id: string; supplierId?: string; parcelIds: string[]; status: "planning" | "consolidated" | "shipped" | "received"; domesticFreightUsd: number; internationalFreightUsd: number; dutiesUsd: number; customsUsd: number; allocationMethod: LandedCostAllocationMethod; createdAt: string; updatedAt?: string };
export type ReceivingSession = { id: string; purchaseOrderId: string; parcelId?: string; status: "draft" | "partial" | "completed" | "issue"; receivedAt: string; rows: { id: string; purchaseOrderItemId: string; variantId: string; expectedQuantity: number; receivedQuantity: number; damagedQuantity: number; shortageQuantity: number; overageQuantity: number; lotId?: string; notes?: string }[]; claimId?: string; idempotencyKey?: string; createdAt: string; updatedAt?: string };
export type SupplierClaim = { id: string; supplierId: string; purchaseOrderId?: string; receivingSessionId?: string; type: "shortage" | "overage" | "damaged" | "quality" | "late"; status: "open" | "submitted" | "approved" | "rejected" | "credited" | "closed"; quantity?: number; amountUsd?: number; detail: string; openedAt: string; resolvedAt?: string; activity: string[] };
export type SupplierPriceHistory = { id: string; supplierId: string; variantId: string; currency: "USD" | "RMB"; unitCostOriginal: number; exchangeRate: number; unitCostUsd: number; minimumOrderQuantity?: number; capturedAt: string; sourceUrl?: string };
export type ReorderRecommendation = { id: string; variantId: string; supplierId?: string; recommendedQuantity: number; reorderPoint: number; safetyStock: number; available: number; incoming: number; velocity30d: number; estimatedCostUsd: number; status: "open" | "approved" | "converted" | "dismissed"; purchaseOrderId?: string; createdAt: string; updatedAt?: string };
export type AnalyticsSavedReport = { id: string; name: string; description?: string; sections: string[]; metrics: string[]; filters: Record<string, string>; drilldowns: ("sku" | "supplier" | "lot" | "marketplace" | "order" | "finance" | "fulfillment")[]; schedule?: { frequency: "none" | "daily" | "weekly" | "monthly"; recipients: string[]; nextRunAt?: string }; exportFormat: "csv"; isDefault?: boolean; createdBy?: string; lastRunAt?: string; createdAt: string; updatedAt?: string };
export type AnalyticsFilterPreset = { id: string; name: string; filters: Record<string, string>; isDefault?: boolean; createdAt: string; updatedAt?: string };
export type AnalyticsReportRun = { id: string; reportId: string; status: "queued" | "completed" | "failed"; filters: Record<string, string>; exportedRowCount: number; createdAt: string; completedAt?: string; error?: string };
export type AutomationTriggerType = "inventory.stock_below_reorder_point" | "inventory.stockout_risk" | "inventory.risk_lock_created" | "inventory.incoming_stock_delayed" | "inventory.aging_threshold" | "inventory.cycle_count_discrepancy" | "orders.order_imported" | "orders.payment_received" | "orders.reservation_failed" | "orders.ready_to_pack" | "orders.return_requested" | "orders.refund_requested" | "orders.shipping_deadline_approaching" | "fulfillment.pick_delayed" | "fulfillment.pack_delayed" | "fulfillment.shipment_dispatched" | "fulfillment.tracking_stalled" | "fulfillment.exception_created" | "fulfillment.return_to_sender" | "finance.payout_received" | "finance.payout_discrepancy" | "finance.deployable_cash_above_threshold" | "finance.tax_reserve_below_target" | "finance.expense_above_threshold" | "finance.budget_exceeded" | "finance.negative_margin_detected" | "purchasing.reorder_recommendation_created" | "purchasing.supplier_lead_time_worsens" | "purchasing.po_approval_required" | "purchasing.parcel_delayed" | "purchasing.claim_unresolved" | "purchasing.landed_cost_exceeds_target" | "listings.publish_failed" | "listings.quantity_sync_failed" | "listings.channel_listing_stale" | "listings.sibling_delist_required" | "listings.risk_lock_triggered" | "listings.performance_below_threshold" | "analytics.kpi_crosses_threshold" | "analytics.saved_report_completed" | "analytics.forecast_variance_exceeds_threshold" | "analytics.dead_stock_detected" | "analytics.channel_margin_drops" | "schedule";
export type AutomationOperator = "equals" | "not_equals" | "greater_than" | "less_than" | "greater_or_equal" | "less_or_equal" | "contains" | "does_not_contain" | "in_list" | "not_in_list" | "exists" | "does_not_exist" | "age_exceeds" | "percentage_change_exceeds" | "date_time_window";
export type AutomationActionType = "create_notification" | "create_task" | "send_email_boundary" | "create_reorder_recommendation" | "draft_purchase_order" | "request_approval" | "reserve_inventory" | "release_reservation" | "place_sku_risk_lock" | "release_risk_lock" | "pause_listing" | "queue_quantity_sync" | "queue_sibling_delist" | "create_fulfillment_exception" | "place_order_on_hold" | "create_expense" | "move_tax_reserve" | "update_budget_alert" | "run_saved_report" | "trigger_forecast_refresh" | "call_webhook_boundary" | "enqueue_background_job";
export type AutomationCondition = { id: string; field: string; operator: AutomationOperator; value?: string | number | boolean | string[]; result?: boolean };
export type AutomationAction = { id: string; type: AutomationActionType; config: Record<string, string | number | boolean | undefined>; approvalRequired?: boolean; destructive?: boolean; financial?: boolean };
export type AutomationSchedule = { id: string; frequency: "one_time" | "hourly" | "daily" | "weekly" | "monthly" | "cron"; cron?: string; timezone: string; businessHoursOnly?: boolean; nextRunAt?: string; createdAt: string; updatedAt?: string };
export type AutomationRule = { id: string; name: string; description?: string; enabled: boolean; owner?: string; priority: number; trigger: { type: AutomationTriggerType; samplePayload: Record<string, unknown> }; conditionMode: "AND" | "OR"; conditions: AutomationCondition[]; actions: AutomationAction[]; schedule?: AutomationSchedule; approvalRequired: boolean; approverRole?: "owner" | "admin" | "finance" | "operations"; dryRun: boolean; archivedAt?: string; lastRunAt?: string; nextRunAt?: string; runCount: number; failureCount: number; createdAt: string; updatedAt?: string };
export type AutomationApproval = { id: string; ruleId: string; runId?: string; actionId?: string; status: "pending" | "approved" | "rejected" | "expired"; approverRole: string; requestedAt: string; decidedAt?: string; decisionBy?: string; editedPayload?: Record<string, unknown>; history: string[] };
export type AutomationStep = { id: string; runId: string; actionId?: string; label: string; status: "pending" | "waiting_approval" | "running" | "succeeded" | "failed" | "skipped" | "cancelled"; attempts: number; maxAttempts: number; nextAttemptAt?: string; startedAt?: string; finishedAt?: string; error?: string; logs: string[]; linkedRecords: { type: string; id: string; href?: string }[] };
export type AutomationRun = { id: string; ruleId: string; triggerType: AutomationTriggerType; status: "queued" | "running" | "waiting_approval" | "succeeded" | "failed" | "dead_lettered" | "cancelled" | "dry_run"; idempotencyKey: string; eventPayload: Record<string, unknown>; conditionResults: AutomationCondition[]; stepIds: string[]; durationMs?: number; error?: string; createdAt: string; startedAt?: string; finishedAt?: string };
export type AutomationRetry = { id: string; runId: string; stepId?: string; attempt: number; runAfter: string; status: "scheduled" | "running" | "succeeded" | "failed" | "cancelled"; error?: string; createdAt: string };
export type AutomationDeadLetter = { id: string; runId: string; ruleId: string; reason: string; payload: Record<string, unknown>; status: "open" | "retried" | "cancelled"; createdAt: string; resolvedAt?: string };
export type AutomationTemplate = { id: string; name: string; description: string; triggerType: AutomationTriggerType; conditions: AutomationCondition[]; actions: AutomationAction[]; approvalRequired: boolean; createdAt: string };
export type AutomationIdempotencyReceipt = { id: string; key: string; ruleId: string; runId: string; createdAt: string };

export type OperatingData = { version: 1; mode: "empty" | "development_demo" | "local"; products: Product[]; variants: Variant[]; locations: Location[]; balances: StockBalance[]; stockMovements: StockMovement[]; suppliers: Supplier[]; purchaseOrders: PurchaseOrder[]; parcels: Parcel[]; listings: Listing[]; customers: Customer[]; orders: Order[]; transactions: Transaction[]; tasks: Task[]; notices: Notice[]; insights: Insight[]; activity: Activity[]; financialAccounts?: FinancialAccount[]; financeCategories?: FinanceCategory[]; payouts?: Payout[]; payoutReconciliations?: PayoutReconciliation[]; expenses?: Expense[]; budgets?: Budget[]; taxReserveMovements?: TaxReserveMovement[]; reinvestmentAllocations?: ReinvestmentAllocation[]; forecasts?: Forecast[]; orderImportReviews?: OrderImportReview[]; orderImportBatches?: OrderImportBatch[]; savedOrderViews?: SavedOrderView[]; fulfillmentShipments?: FulfillmentShipment[]; fulfillmentPickLists?: FulfillmentPickList[]; fulfillmentExceptions?: FulfillmentException[]; fulfillmentManifests?: FulfillmentManifest[]; purchaseBatches?: InventoryPurchaseBatch[]; inventoryLots?: InventoryLot[]; landedCostComponents?: LandedCostComponent[]; exchangeRates?: ExchangeRateRecord[]; orderItemCostAllocations?: OrderItemCostAllocation[]; journalEntries?: JournalEntry[]; journalLines?: JournalLine[]; outboxEvents?: TransactionalOutboxEvent[]; durableJobs?: DurableJob[]; deadLetters?: DeadLetterEvent[]; channelSyncStates?: ChannelInventorySyncState[]; inventoryRiskLocks?: InventoryRiskLock[]; physicalSkuMappings?: PhysicalSkuMapping[]; marketplaceAccounts?: MarketplaceAccount[]; listingTemplates?: ListingTemplate[]; channelListingDrafts?: ChannelListingDraft[]; listingSyncJobs?: ListingSyncJob[]; listingReviewItems?: ListingReviewItem[]; supplierContacts?: SupplierContact[]; supplierCommunications?: SupplierCommunication[]; supplierScorecards?: SupplierScorecard[]; purchaseApprovals?: PurchaseApproval[]; purchasePayments?: PurchasePayment[]; freightConsolidations?: FreightConsolidation[]; receivingSessions?: ReceivingSession[]; supplierClaims?: SupplierClaim[]; supplierPriceHistory?: SupplierPriceHistory[]; reorderRecommendations?: ReorderRecommendation[]; analyticsSavedReports?: AnalyticsSavedReport[]; analyticsFilterPresets?: AnalyticsFilterPreset[]; analyticsReportRuns?: AnalyticsReportRun[]; automationRules?: AutomationRule[]; automationRuns?: AutomationRun[]; automationSteps?: AutomationStep[]; automationApprovals?: AutomationApproval[]; automationRetries?: AutomationRetry[]; automationDeadLetters?: AutomationDeadLetter[]; automationTemplates?: AutomationTemplate[]; automationIdempotencyReceipts?: AutomationIdempotencyReceipt[]; updatedAt: string };
