# Security

The active-business cookie is a preference, not authorization. Server repositories resolve authenticated memberships each access. RLS is enabled for tenant tables and role-aware write policies separate catalog/operations, fulfillment, finance, and owner-only membership responsibilities. Apply migrations and verify with test users before production.
