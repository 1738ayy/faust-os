# Deployment

Use Node/npm compatible with the committed lockfile. Set public Supabase URL and anon key, enable production auth, configure Supabase redirects, apply migrations in order, and keep service credentials server-only. Run the full validation suite before release.
