# Marketplace adapters

Each adapter validates a listing, publishes it, ends it, and may import orders. The manual adapter provides the truthful fallback: copy title/description/price, upload images, paste external URL/ID, and confirm active status. API publication requires marketplace credentials and approval.
